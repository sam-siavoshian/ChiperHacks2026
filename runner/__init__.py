"""Arena match runner — boots the arena, spawns both Claude Code sessions, drives
turns, sets each side's clock, and frames the broadcast. See README.md."""

__all__ = ["config", "clients", "processes", "session", "match"]
